  docker.enabled = true
  docker.runOptions = "-i -v ${baseDir}:${baseDir}"
  process.container = "dataintuitive/viash"
  params {
    calculate_totalvi_metrics__input_prediction = "viash_no_value"
    calculate_totalvi_metrics__input_solution = "viash_no_value"
    calculate_totalvi_metrics__output = "viash_no_value"
    calculate_totalvi_metrics__n_neighbors = "100"
    id = ""
    testScript = ""
    testResources = [  ]
    calculate_totalvi_metrics {
      name = "calculate_totalvi_metrics"
      container = "joint_embedding_metrics_calculate_totalvi_metrics"
      containerTag = "main_build"
      containerRegistry = "openproblems"
      command = "calculate_totalvi_metrics"
      tests {
        isDefined = false
        testScript = "NA"
        testResources = [  ]
      }
      arguments {
        input_prediction {
          name = "input_prediction"
          otype = "--"
          required = true
          type = "file"
          direction = "Input"
          multiple = false
          multiple_sep = ":"
          value = "${params.calculate_totalvi_metrics__input_prediction}"
          example = "prediction.h5ad"
          description = "Prediction by a method"
        }
        input_solution {
          name = "input_solution"
          otype = "--"
          required = true
          type = "file"
          direction = "Input"
          multiple = false
          multiple_sep = ":"
          value = "${params.calculate_totalvi_metrics__input_solution}"
          example = "solution.h5ad"
          description = "Gold standard solution"
        }
        output {
          name = "output"
          otype = "--"
          required = true
          type = "file"
          direction = "Output"
          multiple = false
          multiple_sep = ":"
          value = "${params.calculate_totalvi_metrics__output}"
          example = "output.h5ad"
          description = "Latent mixing metric measures how well the latent cell representations are mixed between batches relative to the global frequency of batches."
        }
        n_neighbors {
          name = "n_neighbors"
          otype = "--"
          required = false
          type = "integer"
          direction = "Input"
          multiple = false
          multiple_sep = ":"
          value = "${params.calculate_totalvi_metrics__n_neighbors}"
          dflt = "100"
          description = "Number of neighbors for the entropy_batch_mixing metric."
        }
      }
    }
  }