functionality:
  name: "create_starter_kit"
  namespace: "common"
  version: "main_build"
  authors:
  - name: "Robrecht Cannoodt"
    email: "rcannood@gmail.com"
    roles:
    - "maintainer"
    props:
      github: "rcannood"
      orcid: "0000-0003-3641-729X"
  arguments:
  - type: "file"
    name: "--input_dir"
    alternatives: []
    example: "src/predict_modality/starter_kit/starter_kit_r/"
    must_exist: false
    required: true
    direction: "Input"
    multiple: false
    multiple_sep: ":"
  - type: "string"
    name: "--task"
    alternatives: []
    example: "predict_modality"
    required: true
    direction: "Input"
    multiple: false
    multiple_sep: ":"
  - type: "string"
    name: "--task_name"
    alternatives: []
    example: "Predict Modality"
    required: true
    direction: "Input"
    multiple: false
    multiple_sep: ":"
  - type: "string"
    name: "--language"
    alternatives: []
    example: "r"
    required: true
    direction: "Input"
    multiple: false
    multiple_sep: ":"
  - type: "string"
    name: "--language_name"
    alternatives: []
    example: "R"
    required: true
    direction: "Input"
    multiple: false
    multiple_sep: ":"
  - type: "string"
    name: "--evalai_phase"
    alternatives: []
    example: "2276"
    required: true
    direction: "Input"
    multiple: false
    multiple_sep: ":"
  - type: "string"
    name: "--memory"
    alternatives: []
    default: "16 GB"
    required: false
    direction: "Input"
    multiple: false
    multiple_sep: ":"
  - type: "string"
    name: "--time"
    alternatives: []
    default: "10m"
    required: false
    direction: "Input"
    multiple: false
    multiple_sep: ":"
  - type: "string"
    name: "--cpus"
    alternatives: []
    default: "4"
    required: false
    direction: "Input"
    multiple: false
    multiple_sep: ":"
  - type: "string"
    name: "--pipeline_version"
    alternatives: []
    default: "0.4.0"
    required: false
    direction: "Input"
    multiple: false
    multiple_sep: ":"
  - type: "file"
    name: "--output_dir"
    alternatives: []
    default: "output/starter_kits"
    must_exist: false
    required: false
    direction: "Output"
    multiple: false
    multiple_sep: ":"
  resources:
  - type: "bash_script"
    text: |
      #!/usr/bin/env bash
      
      #######################################
      #    create_starter_kit main_build    #
      #######################################
      
      # This wrapper script is auto-generated by viash 0.5.1 and is thus a derivative
      # work thereof. This software comes with ABSOLUTELY NO WARRANTY from Data
      # Intuitive.  The component may contain files which fall under a different
      # license. The authors of this component should specify the license in the
      # header of such files, or include a separate license file detailing the
      # licenses of all included files.
      #
      # Component authors:
      # * Robrecht Cannoodt <rcannood@gmail.com> (maintainer) {github: rcannood, orcid: 0000-0003-3641-729X}
      
      set -e
      
      if [ -z "$VIASH_TEMP" ]; then
        VIASH_TEMP=/tmp
      fi
      
      # define helper functions
      # ViashQuote: put quotes around non flag values
      # $1     : unquoted string
      # return : possibly quoted string
      # examples:
      #   ViashQuote --foo      # returns --foo
      #   ViashQuote bar        # returns 'bar'
      #   Viashquote --foo=bar  # returns --foo='bar'
      function ViashQuote {
        if [[ "$1" =~ ^-+[a-zA-Z0-9_\-]+=.+$ ]]; then
          echo "$1" | sed "s#=\(.*\)#='\1'#"
        elif [[ "$1" =~ ^-+[a-zA-Z0-9_\-]+$ ]]; then
          echo "$1"
        else
          echo "'$1'"
        fi
      }
      # ViashRemoveFlags: Remove leading flag
      # $1     : string with a possible leading flag
      # return : string without possible leading flag
      # examples:
      #   ViashRemoveFlags --foo=bar  # returns bar
      function ViashRemoveFlags {
        echo "$1" | sed 's/^--*[a-zA-Z0-9_\-]*=//'
      }
      # ViashSourceDir: return the path of a bash file, following symlinks
      # usage   : ViashSourceDir ${BASH_SOURCE[0]}
      # $1      : Should always be set to ${BASH_SOURCE[0]}
      # returns : The absolute path of the bash file
      function ViashSourceDir {
        SOURCE="$1"
        while [ -h "$SOURCE" ]; do
          DIR="$( cd -P "$( dirname "$SOURCE" )" >/dev/null 2>&1 && pwd )"
          SOURCE="$(readlink "$SOURCE")"
          [[ $SOURCE != /* ]] && SOURCE="$DIR/$SOURCE"
        done
        cd -P "$( dirname "$SOURCE" )" >/dev/null 2>&1 && pwd
      }
      VIASH_VERBOSITY=5
      
      # see https://en.wikipedia.org/wiki/Syslog#Severity_level
      
      # ViashLog: Log events depending on the verbosity level
      # usage: ViashLog 1 alert Oh no something went wrong!
      # $1: required verbosity level
      # $2: display tag
      # $3+: messages to display
      # stdout: Your input, prepended by '[$2] '. 
      function ViashLog {
        local required_level="$1"
        local display_tag="$2"
        shift 2
        if [ $VIASH_VERBOSITY -ge $required_level ]; then
          echo "[$display_tag]" "$@"
        fi
      }
      
      # ViashEmergency: log events when the system is unstable
      # usage: ViashEmergency Oh no something went wrong.
      # stdout: Your input, prepended by '[emergency] '. 
      function ViashEmergency {
        ViashLog 0 emergency $@
      }
      
      # ViashAlert: log events when actions must be taken immediately (e.g. corrupted system database)
      # usage: ViashAlert Oh no something went wrong.
      # stdout: Your input, prepended by '[alert] '. 
      function ViashAlert {
        ViashLog 1 alert $@
      }
      
      # ViashCritical: log events when a critical condition occurs
      # usage: ViashCritical Oh no something went wrong.
      # stdout: Your input, prepended by '[critical] '. 
      function ViashCritical {
        ViashLog 2 critical $@
      }
      
      # ViashError: log events when an error condition occurs
      # usage: ViashError Oh no something went wrong.
      # stdout: Your input, prepended by '[error] '. 
      function ViashError {
        ViashLog 3 error $@
      }
      
      # ViashWarning: log potentially abnormal events
      # usage: ViashWarning Something may have gone wrong.
      # stdout: Your input, prepended by '[warning] '. 
      function ViashWarning {
        ViashLog 4 warning $@
      }
      
      # ViashNotice: log significant but normal events
      # usage: ViashNotice This just happened.
      # stdout: Your input, prepended by '[notice] '. 
      function ViashNotice {
        ViashLog 5 notice $@
      }
      
      # ViashInfo: log normal events
      # usage: ViashInfo This just happened.
      # stdout: Your input, prepended by '[info] '. 
      function ViashInfo {
        ViashLog 6 info $@
      }
      
      # ViashDebug: log all events, for debugging purposes
      # usage: ViashDebug This just happened.
      # stdout: Your input, prepended by '[debug] '. 
      function ViashDebug {
        ViashLog 7 debug $@
      }
      
      # find source folder of this component
      VIASH_RESOURCES_DIR=`ViashSourceDir ${BASH_SOURCE[0]}`
      
      
      # ViashHelp: Display helpful explanation about this executable
      function ViashHelp {
        echo "create_starter_kit main_build"
        echo "Create a starter kit."
        echo
        echo "Options:"
      
        echo "   --input_dir"
        echo "        type: file, required parameter"
        echo "        example: src/predict_modality/starter_kit/starter_kit_r/"
        echo ""
      
      
        echo "   --task"
        echo "        type: string, required parameter"
        echo "        example: predict_modality"
        echo ""
      
      
        echo "   --task_name"
        echo "        type: string, required parameter"
        echo "        example: Predict Modality"
        echo ""
      
      
        echo "   --language"
        echo "        type: string, required parameter"
        echo "        example: r"
        echo ""
      
      
        echo "   --language_name"
        echo "        type: string, required parameter"
        echo "        example: R"
        echo ""
      
      
        echo "   --evalai_phase"
        echo "        type: string, required parameter"
        echo "        example: 2276"
        echo ""
      
      
        echo "   --memory"
        echo "        type: string"
        echo "        default: 16 GB"
        echo ""
      
      
        echo "   --time"
        echo "        type: string"
        echo "        default: 10m"
        echo ""
      
      
        echo "   --cpus"
        echo "        type: string"
        echo "        default: 4"
        echo ""
      
      
        echo "   --pipeline_version"
        echo "        type: string"
        echo "        default: 0.4.0"
        echo ""
      
      
        echo "   --output_dir"
        echo "        type: file, output"
        echo "        default: output/starter_kits"
        echo ""
      
      }
      
      # initialise array
      VIASH_POSITIONAL_ARGS=''
      
      while [[ $# -gt 0 ]]; do
          case "$1" in
              -h|--help)
                  ViashHelp
                  exit
                  ;;
              -v|--verbose)
                  let "VIASH_VERBOSITY=VIASH_VERBOSITY+1"
                  shift 1
                  ;;
              -vv)
                  let "VIASH_VERBOSITY=VIASH_VERBOSITY+2"
                  shift 1
                  ;;
              --verbosity)
                  VIASH_VERBOSITY="$2"
                  shift 2
                  ;;
              --verbosity=*)
                  VIASH_VERBOSITY="$(ViashRemoveFlags "$1")"
                  shift 1
                  ;;
              --version)
                  echo "create_starter_kit main_build"
                  exit
                  ;;
              --input_dir)
                  VIASH_PAR_INPUT_DIR="$2"
                  shift 2
                  ;;
              --input_dir=*)
                  VIASH_PAR_INPUT_DIR=$(ViashRemoveFlags "$1")
                  shift 1
                  ;;
              --task)
                  VIASH_PAR_TASK="$2"
                  shift 2
                  ;;
              --task=*)
                  VIASH_PAR_TASK=$(ViashRemoveFlags "$1")
                  shift 1
                  ;;
              --task_name)
                  VIASH_PAR_TASK_NAME="$2"
                  shift 2
                  ;;
              --task_name=*)
                  VIASH_PAR_TASK_NAME=$(ViashRemoveFlags "$1")
                  shift 1
                  ;;
              --language)
                  VIASH_PAR_LANGUAGE="$2"
                  shift 2
                  ;;
              --language=*)
                  VIASH_PAR_LANGUAGE=$(ViashRemoveFlags "$1")
                  shift 1
                  ;;
              --language_name)
                  VIASH_PAR_LANGUAGE_NAME="$2"
                  shift 2
                  ;;
              --language_name=*)
                  VIASH_PAR_LANGUAGE_NAME=$(ViashRemoveFlags "$1")
                  shift 1
                  ;;
              --evalai_phase)
                  VIASH_PAR_EVALAI_PHASE="$2"
                  shift 2
                  ;;
              --evalai_phase=*)
                  VIASH_PAR_EVALAI_PHASE=$(ViashRemoveFlags "$1")
                  shift 1
                  ;;
              --memory)
                  VIASH_PAR_MEMORY="$2"
                  shift 2
                  ;;
              --memory=*)
                  VIASH_PAR_MEMORY=$(ViashRemoveFlags "$1")
                  shift 1
                  ;;
              --time)
                  VIASH_PAR_TIME="$2"
                  shift 2
                  ;;
              --time=*)
                  VIASH_PAR_TIME=$(ViashRemoveFlags "$1")
                  shift 1
                  ;;
              --cpus)
                  VIASH_PAR_CPUS="$2"
                  shift 2
                  ;;
              --cpus=*)
                  VIASH_PAR_CPUS=$(ViashRemoveFlags "$1")
                  shift 1
                  ;;
              --pipeline_version)
                  VIASH_PAR_PIPELINE_VERSION="$2"
                  shift 2
                  ;;
              --pipeline_version=*)
                  VIASH_PAR_PIPELINE_VERSION=$(ViashRemoveFlags "$1")
                  shift 1
                  ;;
              --output_dir)
                  VIASH_PAR_OUTPUT_DIR="$2"
                  shift 2
                  ;;
              --output_dir=*)
                  VIASH_PAR_OUTPUT_DIR=$(ViashRemoveFlags "$1")
                  shift 1
                  ;;
              *)  # positional arg or unknown option
                  # since the positional args will be eval'd, can we always quote, instead of using ViashQuote
                  VIASH_POSITIONAL_ARGS="$VIASH_POSITIONAL_ARGS '$1'"
                  shift # past argument
                  ;;
          esac
      done
      
      # parse positional parameters
      eval set -- $VIASH_POSITIONAL_ARGS
      
      
      
      # check whether required parameters exist
      if [ -z "$VIASH_PAR_INPUT_DIR" ]; then
        ViashError '--input_dir' is a required argument. Use "--help" to get more information on the parameters.
        exit 1
      fi
      if [ -z "$VIASH_PAR_TASK" ]; then
        ViashError '--task' is a required argument. Use "--help" to get more information on the parameters.
        exit 1
      fi
      if [ -z "$VIASH_PAR_TASK_NAME" ]; then
        ViashError '--task_name' is a required argument. Use "--help" to get more information on the parameters.
        exit 1
      fi
      if [ -z "$VIASH_PAR_LANGUAGE" ]; then
        ViashError '--language' is a required argument. Use "--help" to get more information on the parameters.
        exit 1
      fi
      if [ -z "$VIASH_PAR_LANGUAGE_NAME" ]; then
        ViashError '--language_name' is a required argument. Use "--help" to get more information on the parameters.
        exit 1
      fi
      if [ -z "$VIASH_PAR_EVALAI_PHASE" ]; then
        ViashError '--evalai_phase' is a required argument. Use "--help" to get more information on the parameters.
        exit 1
      fi
      if [ -z "$VIASH_PAR_MEMORY" ]; then
        VIASH_PAR_MEMORY="16 GB"
      fi
      if [ -z "$VIASH_PAR_TIME" ]; then
        VIASH_PAR_TIME="10m"
      fi
      if [ -z "$VIASH_PAR_CPUS" ]; then
        VIASH_PAR_CPUS="4"
      fi
      if [ -z "$VIASH_PAR_PIPELINE_VERSION" ]; then
        VIASH_PAR_PIPELINE_VERSION="0.4.0"
      fi
      if [ -z "$VIASH_PAR_OUTPUT_DIR" ]; then
        VIASH_PAR_OUTPUT_DIR="output/starter_kits"
      fi
      
      
      cat << VIASHEOF | bash
      set -e
      tempscript=\$(mktemp "$VIASH_TEMP/viash-run-create_starter_kit-XXXXXX")
      function clean_up {
        rm "\$tempscript"
      }
      trap clean_up EXIT
      cat > "\$tempscript" << 'VIASHMAIN'
      #!/bin/bash
      
      set -ex
      
      ## VIASH START
      # The following code has been auto-generated by Viash.
      par_input_dir='$VIASH_PAR_INPUT_DIR'
      par_task='$VIASH_PAR_TASK'
      par_task_name='$VIASH_PAR_TASK_NAME'
      par_language='$VIASH_PAR_LANGUAGE'
      par_language_name='$VIASH_PAR_LANGUAGE_NAME'
      par_evalai_phase='$VIASH_PAR_EVALAI_PHASE'
      par_memory='$VIASH_PAR_MEMORY'
      par_time='$VIASH_PAR_TIME'
      par_cpus='$VIASH_PAR_CPUS'
      par_pipeline_version='$VIASH_PAR_PIPELINE_VERSION'
      par_output_dir='$VIASH_PAR_OUTPUT_DIR'
      
      resources_dir="$VIASH_RESOURCES_DIR"
      
      ## VIASH END
      
      output_dir="\$par_output_dir/starter_kit-\$par_task-\$par_language/"
      
      echo remove previous results
      [[ -d \$output_dir ]] && rm -r \$output_dir
      
      echo create new output dir
      mkdir -p \$output_dir
      
      echo copy template files
      cp \$resources_dir/template_files/* \$output_dir/
      
      echo replace terms in templates
      sed -i "s#\\\\\\\$par_task_name#\$par_task_name#g" \$output_dir/*
      sed -i "s#\\\\\\\$par_task#\$par_task#g" \$output_dir/*
      sed -i "s#\\\\\\\$par_language_name#\$par_language_name#g" \$output_dir/*
      sed -i "s#\\\\\\\$par_language#\$par_language#g" \$output_dir/*
      sed -i "s#\\\\\\\$par_evalai_phase#\$par_evalai_phase#g" \$output_dir/*
      sed -i "s#\\\\\\\$par_memory#\$par_memory#g" \$output_dir/*
      sed -i "s#\\\\\\\$par_time#\$par_time#g" \$output_dir/*
      sed -i "s#\\\\\\\$par_cpus#\$par_cpus#g" \$output_dir/*
      sed -i "s#\\\\\\\$par_pipeline_version#\$par_pipeline_version#g" \$output_dir/*
      
      echo copy scripts
      cp \$par_input_dir/* \$output_dir
      
      echo copy sample resources
      mkdir -p \$output_dir/sample_data/
      cp \$resources_dir/resources_test/\$par_task/test_resource.mod[12].h5ad \$output_dir/sample_data/
      
      echo copy .gitignore file
      cp \$resources_dir/template_files/.gitignore \$output_dir/
      cp \$resources_dir/template_files/nextflow.config \$output_dir/
      
      echo starter kit is done!
      VIASHMAIN
      PATH="$VIASH_RESOURCES_DIR:\$PATH"
      
      bash "\$tempscript"
      
      VIASHEOF
      

    dest: "create_starter_kit"
    is_executable: true
  - type: "file"
    path: "template_files"
  - type: "file"
    path: "../../../../resources_test"
  - type: "file"
    text: |
        docker.enabled = true
        docker.runOptions = "-i -v ${baseDir}:${baseDir}"
        process.container = "dataintuitive/viash"
        params {
          create_starter_kit__input_dir = "viash_no_value"
          create_starter_kit__task = "viash_no_value"
          create_starter_kit__task_name = "viash_no_value"
          create_starter_kit__language = "viash_no_value"
          create_starter_kit__language_name = "viash_no_value"
          create_starter_kit__evalai_phase = "viash_no_value"
          create_starter_kit__memory = "16 GB"
          create_starter_kit__time = "10m"
          create_starter_kit__cpus = "4"
          create_starter_kit__pipeline_version = "0.4.0"
          create_starter_kit__output_dir = "output/starter_kits"
          id = ""
          testScript = ""
          testResources = [  ]
          create_starter_kit {
            name = "create_starter_kit"
            container = "common_create_starter_kit"
            containerTag = "main_build"
            containerRegistry = "openproblems"
            command = "create_starter_kit"
            tests {
              isDefined = false
              testScript = "NA"
              testResources = [  ]
            }
            arguments {
              input_dir {
                name = "input_dir"
                otype = "--"
                required = true
                type = "file"
                direction = "Input"
                multiple = false
                multiple_sep = ":"
                value = "${params.create_starter_kit__input_dir}"
                example = "src/predict_modality/starter_kit/starter_kit_r/"
              }
              task {
                name = "task"
                otype = "--"
                required = true
                type = "string"
                direction = "Input"
                multiple = false
                multiple_sep = ":"
                value = "${params.create_starter_kit__task}"
                example = "predict_modality"
              }
              task_name {
                name = "task_name"
                otype = "--"
                required = true
                type = "string"
                direction = "Input"
                multiple = false
                multiple_sep = ":"
                value = "${params.create_starter_kit__task_name}"
                example = "Predict Modality"
              }
              language {
                name = "language"
                otype = "--"
                required = true
                type = "string"
                direction = "Input"
                multiple = false
                multiple_sep = ":"
                value = "${params.create_starter_kit__language}"
                example = "r"
              }
              language_name {
                name = "language_name"
                otype = "--"
                required = true
                type = "string"
                direction = "Input"
                multiple = false
                multiple_sep = ":"
                value = "${params.create_starter_kit__language_name}"
                example = "R"
              }
              evalai_phase {
                name = "evalai_phase"
                otype = "--"
                required = true
                type = "string"
                direction = "Input"
                multiple = false
                multiple_sep = ":"
                value = "${params.create_starter_kit__evalai_phase}"
                example = "2276"
              }
              memory {
                name = "memory"
                otype = "--"
                required = false
                type = "string"
                direction = "Input"
                multiple = false
                multiple_sep = ":"
                value = "${params.create_starter_kit__memory}"
                dflt = "16 GB"
              }
              time {
                name = "time"
                otype = "--"
                required = false
                type = "string"
                direction = "Input"
                multiple = false
                multiple_sep = ":"
                value = "${params.create_starter_kit__time}"
                dflt = "10m"
              }
              cpus {
                name = "cpus"
                otype = "--"
                required = false
                type = "string"
                direction = "Input"
                multiple = false
                multiple_sep = ":"
                value = "${params.create_starter_kit__cpus}"
                dflt = "4"
              }
              pipeline_version {
                name = "pipeline_version"
                otype = "--"
                required = false
                type = "string"
                direction = "Input"
                multiple = false
                multiple_sep = ":"
                value = "${params.create_starter_kit__pipeline_version}"
                dflt = "0.4.0"
              }
              output_dir {
                name = "output_dir"
                otype = "--"
                required = false
                type = "file"
                direction = "Output"
                multiple = false
                multiple_sep = ":"
                value = "${params.create_starter_kit__output_dir}"
                dflt = "output/starter_kits"
              }
            }
          }
        }

    dest: "nextflow.config"
  - type: "file"
    text: |
      nextflow.enable.dsl=2
      
      params.test = false
      params.debug = false
      params.publishDir = "./"
      
      // A function to verify (at runtime) if all required arguments are effectively provided.
      def checkParams(_params) {
        _params.arguments.collect{
          if (it.value == "viash_no_value") {
            println("[ERROR] option --${it.name} not specified in component create_starter_kit")
            println("exiting now...")
              exit 1
          }
        }
      }
      
      
      def escape(str) {
        return str.replaceAll('\\\\', '\\\\\\\\').replaceAll("\"", "\\\\\"").replaceAll("\n", "\\\\n").replaceAll("`", "\\\\`")
      }
      
      def renderArg(it) {
        if (it.otype == "") {
          return "'" + escape(it.value) + "'"
        } else if (it.type == "boolean_true") {
          if (it.value.toLowerCase() == "true") {
            return it.otype + it.name
          } else {
            return ""
          }
        } else if (it.type == "boolean_false") {
          if (it.value.toLowerCase() == "true") {
            return ""
          } else {
            return it.otype + it.name
          }
        } else if (it.value == "no_default_value_configured") {
          return ""
        } else {
          def retVal = it.value in List && it.multiple ? it.value.join(it.multiple_sep): it.value
          return it.otype + it.name + " '" + escape(retVal) + "'"
        }
      }
      
      def renderCLI(command, arguments) {
        def argumentsList = arguments.collect{renderArg(it)}.findAll{it != ""}
      
        def command_line = command + argumentsList
      
        return command_line.join(" ")
      }
      
      def effectiveContainer(processParams) {
        def _registry = params.containsKey("containerRegistry") ? params.containerRegistry : processParams.containerRegistry
        def _name = processParams.container
        def _tag = params.containsKey("containerTag") ? params.containerTag : processParams.containerTag
      
        return (_registry == "" ? "" : _registry + "/") + _name + ":" + _tag
      }
      
      // Convert the nextflow.config arguments list to a List instead of a LinkedHashMap
      // The rest of this main.nf script uses the Map form
      def argumentsAsList(_params) {
        def overrideArgs = _params.arguments.collect{ key, value -> value }
        def newParams = _params + [ "arguments" : overrideArgs ]
        return newParams
      }
      
      
      // Use the params map, create a hashmap of the filenames for output
      // output filename is <sample>.<method>.<arg_name>[.extension]
      def outFromIn(_params) {
      
        def id = _params.id
      
        _params
          .arguments
          .findAll{ it -> it.type == "file" && it.direction == "Output" }
          .collect{ it ->
            // If an 'example' attribute is present, strip the extension from the filename,
            // If a 'dflt' attribute is present, strip the extension from the filename,
            // Otherwise just use the option name as an extension.
            def extOrName =
              (it.example != null)
                ? it.example.split(/\./).last()
                : (it.dflt != null)
                  ? it.dflt.split(/\./).last()
                  : it.name
            // The output filename is <sample> . <modulename> . <extension>
            // Unless the output argument is explicitly specified on the CLI
            def newValue =
              (it.value == "viash_no_value")
                ? "create_starter_kit." + it.name + "." + extOrName
                : it.value
            def newName =
              (id != "")
                ? id + "." + newValue
                : it.name + newValue
            it + [ value : newName ]
          }
      
      }
      
      // A process that filters out output_dir from the output Map
      process filterOutput_dir {
      
        input:
          tuple val(id), val(input), val(_params)
        output:
          tuple val(id), val(output), val(_params)
        when:
          input.keySet().contains("output_dir")
        exec:
          output = input["output_dir"]
      
      }
      
      def overrideIO(_params, inputs, outputs) {
      
        // `inputs` in fact can be one of:
        // - `String`,
        // - `List[String]`,
        // - `Map[String, String | List[String]]`
        // Please refer to the docs for more info
        def overrideArgs = _params.arguments.collect{ it ->
          if (it.type == "file") {
            if (it.direction == "Input") {
              (inputs in List || inputs in HashMap)
                ? (inputs in List)
                  ? it + [ "value" : inputs.join(it.multiple_sep)]
                  : (inputs[it.name] != null)
                    ? (inputs[it.name] in List)
                      ? it + [ "value" : inputs[it.name].join(it.multiple_sep)]
                      : it + [ "value" : inputs[it.name]]
                    : it
                : it + [ "value" : inputs ]
            } else {
              (outputs in List || outputs in HashMap)
                ? (outputs in List)
                  ? it + [ "value" : outputs.join(it.multiple_sep)]
                  : (outputs[it.name] != null)
                    ? (outputs[it.name] in List)
                      ? it + [ "value" : outputs[it.name].join(it.multiple_sep)]
                      : it + [ "value" : outputs[it.name]]
                    : it
                : it + [ "value" : outputs ]
            }
          } else {
            it
          }
        }
      
        def newParams = _params + [ "arguments" : overrideArgs ]
      
        return newParams
      
      }
      
      process create_starter_kit_process {
        tag "${id}"
        echo { (params.debug == true) ? true : false }
        cache 'deep'
        stageInMode "symlink"
        container "${container}"
      
        input:
          tuple val(id), path(input), val(output), val(container), val(cli), val(_params)
        output:
          tuple val("${id}"), path(output), val(_params)
        stub:
          """
          # Adding NXF's `$moduleDir` to the path in order to resolve our own wrappers
          export PATH="${moduleDir}:\$PATH"
          STUB=1 $cli
          """
        script:
          if (params.test)
            """
            # Some useful stuff
            export NUMBA_CACHE_DIR=/tmp/numba-cache
            # Running the pre-hook when necessary
            # Adding NXF's `$moduleDir` to the path in order to resolve our own wrappers
            export PATH="./:${moduleDir}:\$PATH"
            ./${params.create_starter_kit.tests.testScript} | tee $output
            """
          else
            """
            # Some useful stuff
            export NUMBA_CACHE_DIR=/tmp/numba-cache
            # Running the pre-hook when necessary
            # Adding NXF's `$moduleDir` to the path in order to resolve our own wrappers
            export PATH="${moduleDir}:\$PATH"
            $cli
            """
      }
      
      workflow create_starter_kit {
      
        take:
        id_input_params_
      
        main:
      
        def key = "create_starter_kit"
      
        def id_input_output_function_cli_params_ =
          id_input_params_.map{ id, input, _params ->
      
            // Start from the (global) params and overwrite with the (local) _params
            def defaultParams = params[key] ? params[key] : [:]
            def overrideParams = _params[key] ? _params[key] : [:]
            def updtParams = defaultParams + overrideParams
            // Convert to List[Map] for the arguments
            def newParams = argumentsAsList(updtParams) + [ "id" : id ]
      
            // Generate output filenames, out comes a Map
            def output = outFromIn(newParams)
      
            // The process expects Path or List[Path], Maps need to be converted
            def inputsForProcess =
              (input in HashMap)
                ? input.collect{ k, v -> v }.flatten()
                : input
            def outputsForProcess = output.collect{ it.value }
      
            // For our machinery, we convert Path -> String in the input
            def inputs =
              (input in List || input in HashMap)
                ? (input in List)
                  ? input.collect{ it.name }
                  : input.collectEntries{ k, v -> [ k, (v in List) ? v.collect{it.name} : v.name ] }
                : input.name
            outputs = output.collectEntries{ [(it.name): it.value] }
      
            def finalParams = overrideIO(newParams, inputs, outputs)
      
            checkParams(finalParams)
      
            new Tuple6(
              id,
              inputsForProcess,
              outputsForProcess,
              effectiveContainer(finalParams),
              renderCLI([finalParams.command], finalParams.arguments),
              finalParams
            )
          }
      
        result_ = create_starter_kit_process(id_input_output_function_cli_params_) \
          | join(id_input_params_) \
          | map{ id, output, _params, input, original_params ->
              def parsedOutput = _params.arguments
                .findAll{ it.type == "file" && it.direction == "Output" }
                .withIndex()
                .collectEntries{ it, i ->
                  // with one entry, output is of type Path and array selections
                  // would select just one element from the path
                  [(it.name): (output in List) ? output[i] : output ]
                }
              new Tuple3(id, parsedOutput, original_params)
            }
      
        result_ \
          | filter { it[1].keySet().size() > 1 } \
          | view{
              ">> Be careful, multiple outputs from this component!"
          }
      
        emit:
        result_.flatMap{ it ->
          (it[1].keySet().size() > 1)
            ? it[1].collect{ k, el -> [ it[0], [ (k): el ], it[2] ] }
            : it[1].collect{ k, el -> [ it[0], el, it[2] ] }
        }
      }
      
      workflow {
        def id = params.id
        def fname = "create_starter_kit"
      
        def _params = params
      
        // could be refactored to be FP
        for (entry in params[fname].arguments) {
          def name = entry.value.name
          if (params[name] != null) {
            params[fname].arguments[name].value = params[name]
          }
        }
      
        def inputFiles = params.create_starter_kit
          .arguments
          .findAll{ key, par -> par.type == "file" && par.direction == "Input" }
          .collectEntries{ key, par -> [(par.name): file(params[fname].arguments[par.name].value) ] }
      
        def ch_ = Channel.from("").map{ s -> new Tuple3(id, inputFiles, params)}
      
        result = create_starter_kit(ch_)
        result.view{ it[1] }
      }
      
      // This workflow is not production-ready yet, we leave it in for future dev
      // TODO
      workflow test {
      
        take:
        rootDir
      
        main:
        params.test = true
        params.create_starter_kit.output = "create_starter_kit.log"
      
        Channel.from(rootDir) \
          | filter { params.create_starter_kit.tests.isDefined } \
          | map{ p -> new Tuple3(
              "tests",
              params.create_starter_kit.tests.testResources.collect{ file( p + it ) },
              params
          )} \
          | create_starter_kit
      
        emit:
        create_starter_kit.out
      }

    dest: "main.nf"
  description: "Create a starter kit."
  tests: []
  info: {}
platform:
  type: "nextflow"
  id: "nextflow"
  registry: "openproblems"
  namespace_separator: "_"
  labels: []
platforms: []
info:
  config: "src/common/create_starter_kits/create_starter_kit/config.vsh.yaml"
  platform: "nextflow"
  output: "target/nextflow/common/create_starter_kit"
  executable: "target/nextflow/common/create_starter_kit/create_starter_kit"
  viash_version: "0.5.1"
  git_commit: "b79432357c9849508b155d634898595f12396e19"
  git_remote: "https://github.com/openproblems-bio/neurips2021_multimodal_viash"
